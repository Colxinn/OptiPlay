# Copy to .env.local and fill values
# --- Database ---
# Use a Postgres connection string in all environments
# e.g. Supabase/Neon/Vercel Postgres
DATABASE_URL="postgresql://user:pass@host:5432/dbname?sslmode=require"

# --- NextAuth core ---
NEXTAUTH_URL="https://optiplay.space"
# Generate one with: `npx auth secret`
NEXTAUTH_SECRET="replace-with-a-secret"

# --- Email sign-in (pick ONE transport) ---
# 
# FOR LOCAL TESTING (Development):
# You can leave AUTH_EMAIL_SERVER empty and magic links will be printed to console
# Just copy the URL from terminal and paste in browser to sign in
#
# FOR PRODUCTION:
# A) MailerSend HTTP API (preferred):
#    - Create and verify a domain/sender in MailerSend
#    - Generate an API token
MAILERSEND_API_TOKEN=""

# B) SMTP transport string (fallback):
#    Formats:
#      smtps://user:pass@smtp.host:465
#      smtp://user:pass@smtp.host:587
AUTH_EMAIL_SERVER=""

# Common (required for production):
# From must be a verified sender, e.g. "OptiPlay <login@yourdomain.com>"
# For local dev, this can be any value like "noreply@localhost"
AUTH_EMAIL_FROM=""

# --- Email policy (optional) ---
# Allow only popular consumer providers by default. To allow any domain set true.
ALLOW_CUSTOM_EMAIL_DOMAINS="false"
# Or provide an allowlist (comma-separated):
ALLOWED_EMAIL_DOMAINS="gmail.com,googlemail.com,outlook.com,hotmail.com,live.com,msn.com,yahoo.com,ymail.com,icloud.com,me.com,proton.me,protonmail.com,aol.com,comcast.net"
# Additional blocked domains (comma-separated) merged with built-in disposable list:
BLOCKED_EMAIL_DOMAINS=""

# --- Owner controls (optional) ---
# Promote these emails to Owner on first login (comma-separated)
OWNER_EMAILS="bigobirdo@gmail.com,trollgebrollgexd@gmail.com,darkenedlight745@gmail.com"

# --- Content safety (optional) ---
# Enable Perspective API pre-flight scanning by supplying your API key.
PERSPECTIVE_API_KEY=""
ENABLE_CONTENT_SCANNER="false"

# --- OAuth (not currently used) ---
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
DISCORD_CLIENT_ID="your-discord-client-id"
DISCORD_CLIENT_SECRET="your-discord-client-secret"
NEXT_PUBLIC_USE_DEV_AUTH=true

# --- Ping telemetry (optional) ---
# Random string used to hash IP addresses before storing ping samples.
PING_HASH_SALT="replace-with-random-string"

# --- Esports Live Data (optional) ---
# Twitch API credentials for live stream verification
# Get from: https://dev.twitch.tv/console/apps
TWITCH_CLIENT_ID=""
TWITCH_CLIENT_SECRET=""

# PandaScore API for real-time esports data
# Free tier available at: https://pandascore.co/
PANDASCORE_API_KEY=""

# --- Google Ads (optional) ---
# Enable Google AdSense ads on the site
# Set to "true" to enable, "false" to disable
NEXT_PUBLIC_GOOGLE_ADS_ENABLED="false"

# Your ad unit slot IDs (get from Google AdSense dashboard)
NEXT_PUBLIC_GOOGLE_ADS_SLOT_ID=""
NEXT_PUBLIC_GOOGLE_ADS_IN_ARTICLE_SLOT_ID=""
